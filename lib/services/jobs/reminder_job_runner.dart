import '../../models/reminder_job.dart';
import 'job_runner.dart';

class ReminderJobRunner extends JobRunner<ReminderJob> {
  @override
  Future<JobRunResult> runJob(ReminderJob job) {
    // TODO: implement runJob
    throw UnimplementedError();
  }
}
